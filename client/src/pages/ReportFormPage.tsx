/**
 * Placeholder for Member 3's ReportFormPage. Member 3 will replace this
 * file's body with the real form + validation. Keeping the default
 * export here means the router boots before that work lands.
 */
export default function ReportFormPage() {
  return (
    <section>
      <h1>Report an Item</h1>
      <p style={{ color: 'var(--text-muted)', marginTop: 8 }}>
        ReportFormPage placeholder &mdash; Member 3 will replace this with
        the real form and validation.
      </p>
    </section>
  );
}
